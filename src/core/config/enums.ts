export enum ConfigPath {
  SERVER = './envs/.env.server',
  TYPEORM = './envs/.env.typeorm',
  TYPEORM_TEST = './envs/.env.typeorm.test',
}

export enum ConfigKey {
  SERVER = 'server',
  TYPEORM = 'typeorm',
  TYPEORM_TEST = 'typeormTest',
}
